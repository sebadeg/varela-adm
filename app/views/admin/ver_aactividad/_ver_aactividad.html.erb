<%= form_tag(admin_aactividad_ver_path, :method => "post") do %>
	<%= select_tag :alumno, options_for_select(Alumno.all.order(:nombre,:apellido).collect{ |u| [u.id + " - " + u.nombre + " " + u.apellido, u.id] }) %>
    <%= submit_tag "Ver" %>
<% end %>
